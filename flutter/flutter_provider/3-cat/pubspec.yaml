name: cat
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http:  ^0.13.4 # ^0.12.0+4 #
  flutter_staggered_grid_view: ^0.4.1
  provider:  ^6.0.1 # ^4.0.4 
  dartz:  ^0.10.0 # ^0.8.9 
  get_it:  ^7.2.0 # ^4.0.1 #
  after_layout: ^1.0.7+2 # ^1.1.0
  google_fonts:  ^2.1.0 # ^0.3.10 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true